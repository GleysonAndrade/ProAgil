/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTimeFormatiPipePipe } from './DateTimeFormatiPipe.pipe';

describe('Pipe: DateTimeFormatiPipee', () => {
  it('create an instance', () => {
    let pipe = new DateTimeFormatiPipePipe();
    expect(pipe).toBeTruthy();
  });
});
